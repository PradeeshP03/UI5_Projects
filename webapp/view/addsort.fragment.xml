<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core" xmlns:f="sap.ui.layout.form" xmlns:l="sap.ui.layout">
<Dialog title="Sort Order">
    <f:SimpleForm>
        <f:content>
            <Button width="15rem"  text="Ascending" press="onAscending"/>
            <Button width="15rem" text="Descending" press="onDescending"/>
        </f:content>
    </f:SimpleForm>
    <!-- <beginButton>
        <Button text="Ascending" press="onAscending"/>
    </beginButton>
    <endButton>
    <Button text="Descending" press="onDescending"/>
    </endButton> -->
     <beginButton>
            <Button text="Close" press="onCloseSortDialog" />
        </beginButton>
</Dialog>
</core:FragmentDefinition>